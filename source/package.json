{
  "name": "db-distribution",
  "version": "1.1.0",
  "description": "Node api for static database distribution",
  "main": "app.js",
  "scripts": {
    "test": "jest --coverage --silent",
    "unit": "jest tests/**/*.js",
    "dev": "node --max-old-space-size=1024 --optimize-for-size app.js",
    "gulp": "gulp",
    "start": "npm run gulp browser-sync",
    "production": "npm install && npm test && npm prune --production"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express-validator": "6.1.1",
    "consign": "0.1.6",
    "express": "4.16.4",
    "express-list-endpoints": "4.0.1",
    "mongoose": "5.3.8",
    "multer": "1.4.1"
  },
  "devDependencies": {
    "@shelf/jest-mongodb": "^1.1.3",
    "babel-jest": "^24.9.0",
    "gulp": "3.9.1",
    "jest": "^24.9.0",
    "jest-transform-stub": "^2.0.0",
    "vue-jest": "^3.0.5"
  }
}
